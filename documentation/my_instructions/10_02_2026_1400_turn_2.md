For question 1:
I want both the correct column mappings (schema matching) and the correct values within those columns (value mappings). As such, the metrics script should indeed report whether the column mapping is correct, and also report within-column metrics. I will decide later about whether to aggregate or average this to a full-table score, for now I want both to be separated. 
So: column names -> new column names (proper gdc schema in this instance)
within-column: proper GDC-compliant values.

For the numeric columns, the original data has these values, and the output data as harmonized by the LLM agent should recapitulate them. Hence, a 'classification' metric is still consistent. Allow an optional tolerance, and an optional amount of digits expected for each number.

I still want exact string matches, since the GDC schema is strict. For now assume truly only one correct match.

Question 3:
For now, we know that the column must be in there for a correct mapping. If it is not there, the LLM did not ge tthe gold standard, and it is a 0 for that column-column mapping and for values within it in principle. However, perhaps the LLM indeed chose another column from the GDC schema.
I will resolve this by having the LLM output a mapping.json that lists initial_colname : mapped_to_colname so that the metric script can take this along.
Fall-back option if that is not present should be to loudly complain about that in the logging and then do metric calculation for metrics that can be calculated.

Question 4:
The rows should be aligned. If they are not, then something went wrong. 
Later tables will probably have an index column or key. Add functionality to check alignment and sort properly for the check based on this index column. 
Note: dou.csv is actually a subset of /hpc/compgen/projects/llm_GEO_project/harmonia_metadata_agent/raw/datasets_harmonia/one_metadata_table_gdc_schema/data/dou-ucec-discovery.csv 
perhaps you can make a dou_with_index.csv, where the Proteomics_Participant_ID is added in, so that we could use that as well if needed. 

Question 5:
Indeed, go for a + c. 

Question 6:
Yes the metrics file should just be a file in that output folder. If the experiment*.py files take care of creating that and we can write to that we are good on this part.

Could you: suggest any other metrics I may want to calculate? And ask me more questions about details I may be overlooking? First think about what those could be in cases such as these.



Question 7 + 8:
in principle the LLM should be exactly correct. After all, the schema is strict. That means that extra whitespace is an error, as is capitalisation.
That said, I agree with these points: for each column, also categorize what fraction of errors are due to whitespace and wrong capitalization only, and output this in the metrics file.

Question 9:
Yes.

Question 11:
I will change the experiment configs and the gold standard to use the .csv with the index column and to compare it against a gold standard that has that selfsame index column if I choose to change the experiment, respectively. You can also make a harmonized_dou_correct_with_index.csv in preparation for this. 

Question 12:
a for now. However if there are columns in the output that were not requested this should somehow count against the model. Report any columns that are outside of the gold standard (just report their count as a metric)

Question 13:
correct. Introduce this config section in all config files. 

For 14: I think the metrics could be a .csv. What is the benefit of JSON? Consider a few formats given all the informations so far, and give me your top 3 recommendations and reasons why. 

Let's just make it entirely JSON. Could you also create a Pydantic schema for this gold standard that the JSON should confirm to so that that is clear and can be used by the visualisation script perhaps? First tell me if that would be useful.

Question 15: set it as an option and log it. Default should be to treat cells that are empty in the gold standard as 'correct'. In-depth view should show that it is simply due to empty being categorized as correct. Perhaps just log the metric both counting and not-counting empty-empty matches.

Question 16:
I guess it should also allow for the possibility of the : null, yes. This is something to discuss slightly more in-depth. Could you do that?

Question 17: just pull them in, yes. A little duplication in text files is not a problem. 

Question 18:
no. 



Actually, scenario B is more nuanced:
It could well be that if there is one column Country, and gdc has both country_of_residence and country_of_birth then without additional information you cannot determine which is correct, and hence both should be considered correct.
I propose the experimental config also has a file path to an harmonization_acceptable_columns.json that lists for certain source columns what acceptable columns are (and this could be more than one). It should be explicit about whether either is correct or whether a column should be split (this may be needed later, for now it should only be one _colname : [correct_gdc_colname_given_limited_info_1, correct_gdc_colname_given_limited_info_2]

16a: yes. Treat differently.
16b: yes, we are logging separate metrics for schema matching (column-column) and value-matching (matching the source values in a column to the proper accepted values in the target schema)
16c: answered above
16d: I assume ideally we make a column_mapping.json and value_mapping.json. Where the latter has per-column source_value:target_value mappings. 
Let's build this into the pipeline, yes. 
We can also test these directly and separately: make me a gold_standard_column_mapping.json and gold_standard_value_mapping.json for this experiment.

Please also change all automated configs in /hpc/compgen/projects/llm_GEO_project/harmonia_metadata_agent/analysis/dstoker/harmonia/experiments/experiment_1_harmonia_dou2020_gdc/configs/automated to properly list what needs to be saved + add in another prompt after saving results/dou_harmonized.csv that tells the LLM to save results/column_mapping.json with source_column : target_column, and results/value_mapping.json with source_column : { unique_val_1_source_col : unique_val_1_target_col, unique_val_2_source_col : unique_val_2_target_col...} etc.

Note: since One-to-many could in principle occur later, please construct a path for it in the code but do not implement anything there yet. If it is called just let it return a NotImplementedError

Is everything clear now?