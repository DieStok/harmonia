As a first step for today's session, we need to update the onboarding document from the codebase 
to the new version of the code. I want you to do the following:
- read the current onboarding document at harmonia_metadata_agent/analysis/dstoker/harmonia/documentation/codebase_descriptions/how_this_codebase_works_15_01_2026.md
- read all code files in harmonia_metadata_agent/analysis/dstoker/harmonia/src and see any changes relative to what the code documentation says
- read the files in the harmonia_metadata_agent/raw/datasets_harmonia folder, especially the readme.md and the experiment_metadata.yaml in each (sub)folder, which describes how the 3 experiments noted in the Harmonia paper should be run, and hold the data needed for each.
- read through the important code for generating experiments, running experiments, and running and building the apptainer image.

Then list all changes needed to the codebase description to onboard a coding agent for the current state of the codebase. With a view towards:
- assume the coding agent is a good engineer who has zero context on this codebase and what we are trying to do
- introduce the current goal: run metadata harmonization agents (using (local) LLMs and beaker-dev) and get performance on specific tasks
- we have two types of experiments: automated experiments where the exact same interaction script is given to each LLM in turn and they complete the interaction and we see how they do (as seen in the current configs in harmonia_metadata_agent/analysis/dstoker/harmonia/experiments/experiment_1_harmonia_dou2020_gdc/configs/automated) and manual ones where we start up the beaker-dev server with the correct LLM backend and a user can interactively run an analysis in this beaker notebook via portforwarding to the hpc (no current configs explicitly for this. )
- we currently still lack metrics and scripts for comparing to ground-truth harmonized metadata or expected formats. This still needs to be implemented.
- the beaker notebook and server run inside an apptainer image (jupyter.sif) on the hpc.

Explicitly specify that:
- for harmonia experiments, the agent should always look in harmonia_metadata_agent/raw/datasets_harmonia
- the agent should always read the latest instructions from the folder harmonia_metadata_agent/analysis/dstoker/harmonia/my_instructions to see what was last done, and ask the user whether that is still up to date or anything has changed.

You HAVE TO CHECK all code files in harmonia_metadata_agent/analysis/dstoker/harmonia/src to check for any function signatures that are different from what is currently described in the code descriptions and list these function signatures explicitly.

Then, make a step-by-step plan for what needs to be changed in the codebase description.

Finally, output the new codebase description in how_this_codebase_works_27_01_2026.md


