# Work Plan Execution Command

Execute a work plan efficiently while maintaining quality and finishing features.

## Introduction

This command takes a work document (plan, specification, or todo file) and executes it systematically. The focus is on **shipping complete features** by understanding requirements quickly, following existing patterns, and maintaining quality throughout.

## Input Document

<input_document> #(use the ce:work skill)

I need you to implement two plans for the Harmonia project. Read both plans carefully, then implement them in order.

## Context

Working directory: /hpc/compgen/projects/llm_GEO_project/harmonia_metadata_agent/analysis/dstoker/harmonia
Python: .venv/bin/python (Python 3.11)
Branch: fix/unified-result-dir-naming
Codebase docs: documentation/codebase_descriptions/how_this_codebase_works_12_03_2026.md

Before starting, read these files to understand the codebase:
- documentation/codebase_descriptions/how_this_codebase_works_12_03_2026.md
- The two plan files below

## Plan 2: Post-Experiment Watcher/Orchestrator

Plan file: documentation/plans/12_03_2026_1115_post_experiment_watcher_orchestrator.md

Create two scripts:
1. `run_post_experiment_analysis.sh` — an sbatch watcher script that chains: log analysis CLI (--json) → calculate_metrics.py for each run → make_standard_evaluation_plots.py --analysis-report. Takes --experiment-name and optional --output-dir. Uses --dependency=afterany to trigger after experiment jobs finish.
2. `launch_experiment.sh` — convenience wrapper that submits the experiment batch job, captures the JOB_ID, and auto-submits the watcher with --dependency=afterany:$JOB_ID.

Key files to reference:
- sbatch_template.sh and sbatch_template_gpu.sh (existing templates for experiment jobs)
- calculate_metrics.py (standalone metrics calculation CLI)
- code_development_tools_agents/monitoring_and_evaluation/read_and_analyze_logs_and_traces_cli.py (log analysis CLI, use --json flag)
- src/evaluation/make_standard_evaluation_plots.py (plot generation, has --analysis-report flag)

Edge cases: handle partial failures (afterany not afterok), all-fail scenario (still generate failure plots), configurable timeout.

## Plan 3: Expand Dashboard with All Visualizations

Plan file: documentation/plans/12_03_2026_1115_expand_dashboard_all_visualizations.md

Add to the existing Dash dashboard (src/dashboard/):
1. New "Failure Analysis" tab (src/dashboard/tabs/failure_analysis.py) — success/failure heatmap, failure distribution bar, failure sunburst
2. New "Error Analysis" tab (src/dashboard/tabs/error_analysis.py) — error breakdown stacked bars, error type pie, per-column error table
3. Enhance Metrics tab with boxplots (groupable by model_family/is_local/cost_tier)
4. Add confusion matrices to Trace Explorer tab
5. Add cross-model comparison heatmaps to Comparison tab
6. Update app.py tab ordering and callbacks

Key files to reference:
- src/dashboard/app.py (main entry, all callbacks)
- src/dashboard/data_loader.py (DashboardDataLoader, dual-source data)
- src/dashboard/tabs/ (existing tab implementations)
- analysis/march11_experiment_plots/dash_app.py (standalone prototype with working failure/error plots — port these)
- src/evaluation/visualization/plots.py (reusable plot functions, including the 4 new failure mode plot functions)
- src/evaluation/visualization/failure_io.py (load_analysis_report, build_all_runs_table)

Data source strategy: data_loader.py already discovers results directories. Enhance it to also load analysis_report.json when available to get failure reasons. Fallback: runs without metrics and without analysis report get "Unknown failure".

## Implementation notes

- Commit each plan separately with a descriptive message
- Run ruff (the pre-commit hook) and fix any issues before committing
- Use plotly backend for all dashboard plots (the dashboard is Plotly Dash)
- Check that existing tests still pass: .venv/bin/python -m pytest tests/ -v
- After implementation, update the codebase description if needed (per CLAUDE.md instructions) </input_document>

## Execution Workflow

### Phase 1: Quick Start

1. **Read Plan and Clarify**

   - Read the work document completely
   - Review any references or links provided in the plan
   - If anything is unclear or ambiguous, ask clarifying questions now
   - Get user approval to proceed
   - **Do not skip this** - better to ask questions now than build the wrong thing

2. **Setup Environment**

   First, check the current branch:

   ```bash
   current_branch=$(git branch --show-current)
   default_branch=$(git symbolic-ref refs/remotes/origin/HEAD 2>/dev/null | sed 's@^refs/remotes/origin/@@')

   # Fallback if remote HEAD isn't set
   if [ -z "$default_branch" ]; then
     default_branch=$(git rev-parse --verify origin/main >/dev/null 2>&1 && echo "main" || echo "master")
   fi
   ```

   **If already on a feature branch** (not the default branch):
   - Ask: "Continue working on `[current_branch]`, or create a new branch?"
   - If continuing, proceed to step 3
   - If creating new, follow Option A or B below

   **If on the default branch**, choose how to proceed:

   **Option A: Create a new branch**
   ```bash
   git pull origin [default_branch]
   git checkout -b feature-branch-name
   ```
   Use a meaningful name based on the work (e.g., `feat/user-authentication`, `fix/email-validation`).

   **Option B: Use a worktree (recommended for parallel development)**
   ```bash
   skill: git-worktree
   # The skill will create a new branch from the default branch in an isolated worktree
   ```

   **Option C: Continue on the default branch**
   - Requires explicit user confirmation
   - Only proceed after user explicitly says "yes, commit to [default_branch]"
   - Never commit directly to the default branch without explicit permission

   **Recommendation**: Use worktree if:
   - You want to work on multiple features simultaneously
   - You want to keep the default branch clean while experimenting
   - You plan to switch between branches frequently

3. **Create Todo List**
   - Use TodoWrite to break plan into actionable tasks
   - Include dependencies between tasks
   - Prioritize based on what needs to be done first
   - Include testing and quality check tasks
   - Keep tasks specific and completable

### Phase 2: Execute

1. **Task Execution Loop**

   For each task in priority order:

   ```
   while (tasks remain):
     - Mark task as in_progress in TodoWrite
     - Read any referenced files from the plan
     - Look for similar patterns in codebase
     - Implement following existing conventions
     - Write tests for new functionality
     - Run System-Wide Test Check (see below)
     - Run tests after changes
     - Mark task as completed in TodoWrite
     - Mark off the corresponding checkbox in the plan file ([ ] → [x])
     - Evaluate for incremental commit (see below)
   ```

   **System-Wide Test Check** — Before marking a task done, pause and ask:

   | Question | What to do |
   |----------|------------|
   | **What fires when this runs?** Callbacks, middleware, observers, event handlers — trace two levels out from your change. | Read the actual code (not docs) for callbacks on models you touch, middleware in the request chain, `after_*` hooks. |
   | **Do my tests exercise the real chain?** If every dependency is mocked, the test proves your logic works *in isolation* — it says nothing about the interaction. | Write at least one integration test that uses real objects through the full callback/middleware chain. No mocks for the layers that interact. |
   | **Can failure leave orphaned state?** If your code persists state (DB row, cache, file) before calling an external service, what happens when the service fails? Does retry create duplicates? | Trace the failure path with real objects. If state is created before the risky call, test that failure cleans up or that retry is idempotent. |
   | **What other interfaces expose this?** Mixins, DSLs, alternative entry points (Agent vs Chat vs ChatMethods). | Grep for the method/behavior in related classes. If parity is needed, add it now — not as a follow-up. |
   | **Do error strategies align across layers?** Retry middleware + application fallback + framework error handling — do they conflict or create double execution? | List the specific error classes at each layer. Verify your rescue list matches what the lower layer actually raises. |

   **When to skip:** Leaf-node changes with no callbacks, no state persistence, no parallel interfaces. If the change is purely additive (new helper method, new view partial), the check takes 10 seconds and the answer is "nothing fires, skip."

   **When this matters most:** Any change that touches models with callbacks, error handling with fallback/retry, or functionality exposed through multiple interfaces.

   **IMPORTANT**: Always update the original plan document by checking off completed items. Use the Edit tool to change `- [ ]` to `- [x]` for each task you finish. This keeps the plan as a living document showing progress and ensures no checkboxes are left unchecked.

2. **Incremental Commits**

   After completing each task, evaluate whether to create an incremental commit:

   | Commit when... | Don't commit when... |
   |----------------|---------------------|
   | Logical unit complete (model, service, component) | Small part of a larger unit |
   | Tests pass + meaningful progress | Tests failing |
   | About to switch contexts (backend → frontend) | Purely scaffolding with no behavior |
   | About to attempt risky/uncertain changes | Would need a "WIP" commit message |

   **Heuristic:** "Can I write a commit message that describes a complete, valuable change? If yes, commit. If the message would be 'WIP' or 'partial X', wait."

   **Commit workflow:**
   ```bash
   # 1. Verify tests pass (use project's test command)
   # Examples: bin/rails test, npm test, pytest, go test, etc.

   # 2. Stage only files related to this logical unit (not `git add .`)
   git add <files related to this logical unit>

   # 3. Commit with conventional message
   git commit -m "feat(scope): description of this unit"
   ```

   **Handling merge conflicts:** If conflicts arise during rebasing or merging, resolve them immediately. Incremental commits make conflict resolution easier since each commit is small and focused.

   **Note:** Incremental commits use clean conventional messages without attribution footers. The final Phase 4 commit/PR includes the full attribution.

3. **Follow Existing Patterns**

   - The plan should reference similar code - read those files first
   - Match naming conventions exactly
   - Reuse existing components where possible
   - Follow project coding standards (see CLAUDE.md)
   - When in doubt, grep for similar implementations

4. **Test Continuously**

   - Run relevant tests after each significant change
   - Don't wait until the end to test
   - Fix failures immediately
   - Add new tests for new functionality
   - **Unit tests with mocks prove logic in isolation. Integration tests with real objects prove the layers work together.** If your change touches callbacks, middleware, or error handling — you need both.

5. **Figma Design Sync** (if applicable)

   For UI work with Figma designs:

   - Implement components following design specs
   - Use figma-design-sync agent iteratively to compare
   - Fix visual differences identified
   - Repeat until implementation matches design

6. **Track Progress**
   - Keep TodoWrite updated as you complete tasks
   - Note any blockers or unexpected discoveries
   - Create new tasks if scope expands
   - Keep user informed of major milestones

### Phase 3: Quality Check

1. **Run Core Quality Checks**

   Always run before submitting:

   ```bash
   # Run full test suite (use project's test command)
   # Examples: bin/rails test, npm test, pytest, go test, etc.

   # Run linting (per CLAUDE.md)
   # Use linting-agent before pushing to origin
   ```

2. **Consider Reviewer Agents** (Optional)

   Use for complex, risky, or large changes. Read agents from `compound-engineering.local.md` frontmatter (`review_agents`). If no settings file, invoke the `setup` skill to create one.

   Run configured agents in parallel with Task tool. Present findings and address critical issues.

3. **Final Validation**
   - All TodoWrite tasks marked completed
   - All tests pass
   - Linting passes
   - Code follows existing patterns
   - Figma designs match (if applicable)
   - No console errors or warnings

4. **Prepare Operational Validation Plan** (REQUIRED)
   - Add a `## Post-Deploy Monitoring & Validation` section to the PR description for every change.
   - Include concrete:
     - Log queries/search terms
     - Metrics or dashboards to watch
     - Expected healthy signals
     - Failure signals and rollback/mitigation trigger
     - Validation window and owner
   - If there is truly no production/runtime impact, still include the section with: `No additional operational monitoring required` and a one-line reason.

### Phase 4: Ship It

1. **Create Commit**

   ```bash
   git add .
   git status  # Review what's being committed
   git diff --staged  # Check the changes

   # Commit with conventional format
   git commit -m "$(cat <<'EOF'
   feat(scope): description of what and why

   Brief explanation if needed.

   🤖 Generated with [Claude Code](https://claude.com/claude-code)

   Co-Authored-By: Claude <noreply@anthropic.com>
   EOF
   )"
   ```

2. **Capture and Upload Screenshots for UI Changes** (REQUIRED for any UI work)

   For **any** design changes, new views, or UI modifications, you MUST capture and upload screenshots:

   **Step 1: Start dev server** (if not running)
   ```bash
   bin/dev  # Run in background
   ```

   **Step 2: Capture screenshots with agent-browser CLI**
   ```bash
   agent-browser open http://localhost:3000/[route]
   agent-browser snapshot -i
   agent-browser screenshot output.png
   ```
   See the `agent-browser` skill for detailed usage.

   **Step 3: Upload using imgup skill**
   ```bash
   skill: imgup
   # Then upload each screenshot:
   imgup -h pixhost screenshot.png  # pixhost works without API key
   # Alternative hosts: catbox, imagebin, beeimg
   ```

   **What to capture:**
   - **New screens**: Screenshot of the new UI
   - **Modified screens**: Before AND after screenshots
   - **Design implementation**: Screenshot showing Figma design match

   **IMPORTANT**: Always include uploaded image URLs in PR description. This provides visual context for reviewers and documents the change.

3. **Create Pull Request**

   ```bash
   git push -u origin feature-branch-name

   gh pr create --title "Feature: [Description]" --body "$(cat <<'EOF'
   ## Summary
   - What was built
   - Why it was needed
   - Key decisions made

   ## Testing
   - Tests added/modified
   - Manual testing performed

   ## Post-Deploy Monitoring & Validation
   - **What to monitor/search**
     - Logs:
     - Metrics/Dashboards:
   - **Validation checks (queries/commands)**
     - `command or query here`
   - **Expected healthy behavior**
     - Expected signal(s)
   - **Failure signal(s) / rollback trigger**
     - Trigger + immediate action
   - **Validation window & owner**
     - Window:
     - Owner:
   - **If no operational impact**
     - `No additional operational monitoring required: <reason>`

   ## Before / After Screenshots
   | Before | After |
   |--------|-------|
   | ![before](URL) | ![after](URL) |

   ## Figma Design
   [Link if applicable]

   ---

   [![Compound Engineered](https://img.shields.io/badge/Compound-Engineered-6366f1)](https://github.com/EveryInc/compound-engineering-plugin) 🤖 Generated with [Claude Code](https://claude.com/claude-code)
   EOF
   )"
   ```

4. **Update Plan Status**

   If the input document has YAML frontmatter with a `status` field, update it to `completed`:
   ```
   status: active  →  status: completed
   ```

5. **Notify User**
   - Summarize what was completed
   - Link to PR
   - Note any follow-up work needed
   - Suggest next steps if applicable

---

## Swarm Mode (Optional)

For complex plans with multiple independent workstreams, enable swarm mode for parallel execution with coordinated agents.

### When to Use Swarm Mode

| Use Swarm Mode when... | Use Standard Mode when... |
|------------------------|---------------------------|
| Plan has 5+ independent tasks | Plan is linear/sequential |
| Multiple specialists needed (review + test + implement) | Single-focus work |
| Want maximum parallelism | Simpler mental model preferred |
| Large feature with clear phases | Small feature or bug fix |

### Enabling Swarm Mode

To trigger swarm execution, say:

> "Make a Task list and launch an army of agent swarm subagents to build the plan"

Or explicitly request: "Use swarm mode for this work"

### Swarm Workflow

When swarm mode is enabled, the workflow changes:

1. **Create Team**
   ```
   Teammate({ operation: "spawnTeam", team_name: "work-{timestamp}" })
   ```

2. **Create Task List with Dependencies**
   - Parse plan into TaskCreate items
   - Set up blockedBy relationships for sequential dependencies
   - Independent tasks have no blockers (can run in parallel)

3. **Spawn Specialized Teammates**
   ```
   Task({
     team_name: "work-{timestamp}",
     name: "implementer",
     subagent_type: "general-purpose",
     prompt: "Claim implementation tasks, execute, mark complete",
     run_in_background: true
   })

   Task({
     team_name: "work-{timestamp}",
     name: "tester",
     subagent_type: "general-purpose",
     prompt: "Claim testing tasks, run tests, mark complete",
     run_in_background: true
   })
   ```

4. **Coordinate and Monitor**
   - Team lead monitors task completion
   - Spawn additional workers as phases unblock
   - Handle plan approval if required

5. **Cleanup**
   ```
   Teammate({ operation: "requestShutdown", target_agent_id: "implementer" })
   Teammate({ operation: "requestShutdown", target_agent_id: "tester" })
   Teammate({ operation: "cleanup" })
   ```

See the `orchestrating-swarms` skill for detailed swarm patterns and best practices.

---

## Key Principles

### Start Fast, Execute Faster

- Get clarification once at the start, then execute
- Don't wait for perfect understanding - ask questions and move
- The goal is to **finish the feature**, not create perfect process

### The Plan is Your Guide

- Work documents should reference similar code and patterns
- Load those references and follow them
- Don't reinvent - match what exists

### Test As You Go

- Run tests after each change, not at the end
- Fix failures immediately
- Continuous testing prevents big surprises

### Quality is Built In

- Follow existing patterns
- Write tests for new code
- Run linting before pushing
- Use reviewer agents for complex/risky changes only

### Ship Complete Features

- Mark all tasks completed before moving on
- Don't leave features 80% done
- A finished feature that ships beats a perfect feature that doesn't

## Quality Checklist

Before creating PR, verify:

- [ ] All clarifying questions asked and answered
- [ ] All TodoWrite tasks marked completed
- [ ] Tests pass (run project's test command)
- [ ] Linting passes (use linting-agent)
- [ ] Code follows existing patterns
- [ ] Figma designs match implementation (if applicable)
- [ ] Before/after screenshots captured and uploaded (for UI changes)
- [ ] Commit messages follow conventional format
- [ ] PR description includes Post-Deploy Monitoring & Validation section (or explicit no-impact rationale)
- [ ] PR description includes summary, testing notes, and screenshots
- [ ] PR description includes Compound Engineered badge

## When to Use Reviewer Agents

**Don't use by default.** Use reviewer agents only when:

- Large refactor affecting many files (10+)
- Security-sensitive changes (authentication, permissions, data access)
- Performance-critical code paths
- Complex algorithms or business logic
- User explicitly requests thorough review

For most features: tests + linting + following patterns is sufficient.

## Common Pitfalls to Avoid

- **Analysis paralysis** - Don't overthink, read the plan and execute
- **Skipping clarifying questions** - Ask now, not after building wrong thing
- **Ignoring plan references** - The plan has links for a reason
- **Testing at the end** - Test continuously or suffer later
- **Forgetting TodoWrite** - Track progress or lose track of what's done
- **80% done syndrome** - Finish the feature, don't move on early
- **Over-reviewing simple changes** - Save reviewer agents for complex work

---

<task-notification>
<task-id>bep986egr</task-id>
<tool-use-id>toolu_0167EWfDN7tfMYHy1Tby81CJ</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>completed</status>
<summary>Background command "Run existing tests" completed (exit code 0)</summary>
</task-notification>
Read the output file to retrieve the result: REDACTED.output

---

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Summary:
1. Primary Request and Intent:
   The user invoked the `/compound-engineering:ce:work` skill requesting implementation of two plans for the Harmonia project, to be implemented in order on branch `fix/unified-result-dir-naming`:
   
   **Plan 2: Post-Experiment Watcher/Orchestrator** — Create two shell scripts:
   - `run_post_experiment_analysis.sh`: An sbatch watcher script that chains log analysis CLI (--json) → calculate_metrics.py for each run → make_standard_evaluation_plots.py --analysis-report. Takes --experiment-name and optional --output-dir. Uses --dependency=afterany.
   - `launch_experiment.sh`: Convenience wrapper that submits experiment batch jobs, captures JOB_IDs, and auto-submits the watcher with --dependency=afterany:$JOB_ID.
   
   **Plan 3: Expand Dashboard with All Visualizations** — Add to the existing Dash dashboard:
   - New "Failure Analysis" tab with success/failure heatmap, failure distribution bar, failure sunburst
   - New "Error Analysis" tab with error breakdown stacked bars, error type pie, per-column error table
   - Enhance Metrics tab with boxplots (groupable by model_family/is_local/cost_tier)
   - Add confusion matrices to Trace Explorer tab
   - Add cross-model comparison heatmaps to Comparison tab
   - Update app.py tab ordering and callbacks
   
   Implementation notes specified: commit each plan separately, run ruff pre-commit hook, use plotly backend, check tests still pass, update codebase description.

2. Key Technical Concepts:
   - SLURM job management (`sbatch`, `--dependency=afterany`, array jobs, `squeue`)
   - Plotly Dash web dashboard architecture (tabs, callbacks, dcc.Store for cross-tab state)
   - Dual-source data loading (file-based results/ scanning + Phoenix HTTP API)
   - Analysis report JSON from `read_and_analyze_logs_and_traces_cli.py --json`
   - Failure mode taxonomy (16-class, from `types_of_log_and_trace_problems.yaml`)
   - Metrics pipeline: `calculate_metrics.py` → `metrics.json` + `row_values.csv`
   - Standard evaluation plots: `make_standard_evaluation_plots.py` with `--analysis-report` flag
   - Run ID system (8-char hex linking logs, results, traces)
   - Pre-commit hooks: ruff (lint+format), shellcheck, yamllint
   - `DashboardDataLoader` class with thread-safe caching and lazy loading
   - `failure_io.py`: `load_analysis_report()`, `build_all_runs_table()` for merging failed/successful runs

3. Files and Code Sections:

   **Files Created:**
   
   - `run_post_experiment_analysis.sh` (new, 218 lines)
     - Watcher sbatch script that runs after experiment jobs complete
     - Parses `--experiment-name`, `--output-dir`, `--backend`, `--num-runs`, `--verbose`
     - Step 1: Runs log analysis CLI with `--json` → `analysis_report.json`
     - Step 2: Finds results dirs matching experiment name, calculates metrics for runs missing `metrics.json` using `.experiment_id` → config path strategy
     - Step 3: Runs `make_standard_evaluation_plots.py` with `--analysis-report` and `--metrics-glob`
     - Step 4: Writes `analysis_complete.json` summary status file
     - SBATCH directives: `--time=00:30:00`, `--mem=8G`, `--account=compgen`

   - `launch_experiment.sh` (new, ~210 lines)
     - Convenience wrapper that submits job scripts and auto-submits watcher
     - Accepts job scripts or directories as positional args
     - `--experiment-name` for analysis grouping (defaults to parsing from job script's RUN_RESULTS_DIR line)
     - `--no-watcher` to skip post-analysis, `--watcher-args` for extra watcher options
     - Builds dependency string from all submitted job IDs: `afterany:id1:id2:id3`
     - Key pattern for experiment name extraction: `grep -oP 'results/\$\{TIMESTAMP\}_\K[^_$]+(?:_[^_$]+)*(?=_\$\{SLURM)' "$FIRST_SCRIPT"`

   - `src/dashboard/tabs/failure_analysis.py` (new)
     - `_success_failure_heatmap(runs_df)`: Model × context grid with OK/FAIL annotations and hover details
     - `_failure_distribution_bar(runs_df)`: Horizontal bar chart of failure reason counts
     - `_failure_sunburst(runs_df)`: Hierarchical sunburst (failure_reason → model → context)
     - `render_failure_analysis(data_loader)`: Main render function with summary cards (total/success/failed/rate)

   - `src/dashboard/tabs/error_analysis.py` (new)
     - `_error_breakdown_stacked(error_df)`: Stacked bar chart of hallucinations/omissions/genuine errors per run
     - `_error_type_pie(error_df)`: Pie chart with donut hole showing aggregate error type distribution
     - `render_error_analysis(data_loader)`: Main render with AG Grid per-column error table

   **Files Modified:**
   
   - `src/dashboard/data_loader.py`
     - Added imports: `infer_context` from `evaluation.visualization.enrich`, `build_all_runs_table` and `load_analysis_report` from `evaluation.visualization.failure_io`
     - Added `get_analysis_report()`: Searches `results/analysis_*/analysis_report.json` (most recent by mtime) and `results/analysis_report.json`, caches result
     - Added `get_all_runs_with_failures()`: Enriches `get_all_runs()` with failure reasons from analysis reports, adds `has_output`, `failure_reason`, `context` columns, defaults to "Unknown failure" for runs without metrics and without known reason
     - Added `get_error_breakdown()`: Returns DataFrame with per-run error counts (hallucinations, omissions, genuine, whitespace, case)
     - Added `get_column_errors()`: Returns DataFrame with per-column per-run error counts
     - Updated `refresh()` to clear `_analysis_report_cache`

   - `src/dashboard/tabs/metrics.py`
     - Added `_metrics_boxplot(metrics_df, group_by)`: Box plots of accuracy/F1/precision/recall grouped by a category, with `boxpoints="all"` and jitter
     - Updated `render_metrics()` to include boxplot section with group-by dropdown (model_family/model/provider)

   - `src/dashboard/tabs/trace_explorer.py`
     - Added `_confusion_matrix_chart(confusion_data, column_name)`: Plotly heatmap confusion matrix with truncated labels
     - Updated `render_trace_explorer()` to show confusion matrices (with column selector dropdown) when metrics are available, filtering out columns with >25 unique values

   - `src/dashboard/tabs/comparison.py`
     - Added `import csv`
     - Added `_cross_model_heatmap(data_loader, run_id_a, run_id_b, column_name)`: Per-row comparison heatmap reading `row_values.csv` for both runs, showing correct/incorrect/empty per row with classification-to-numeric mapping
     - Updated `render_comparison()` to include per-row comparison section with column selector dropdown when both runs have `row_values.csv`

   - `src/dashboard/app.py`
     - Added imports for `render_error_analysis` and `render_failure_analysis`
     - Updated tab ordering: Overview, Metrics, Failure Analysis, Error Analysis, Trace Explorer, Tokens & Cost, Comparison
     - Updated `render_tab()` callback to dispatch to new tabs
     - Added callbacks: `update_boxplot()` (boxplot group selector), `update_confusion_matrix()` (confusion matrix column selector), `update_cross_model_heatmap()` (cross-model column selector), `refresh_failure_analysis()`, `refresh_error_analysis()`, `click_heatmap_to_trace()` (click-through from success/failure heatmap to trace explorer)

   - `documentation/codebase_descriptions/how_this_codebase_works_12_03_2026.md`
     - Updated dashboard section: 5 tabs → 7 tabs with new tab descriptions
     - Added post-experiment orchestration documentation
     - Updated directory tree to include `launch_experiment.sh` and `run_post_experiment_analysis.sh`
     - Updated tabs/ comment to list all 7 tab files

   **Files Read (key references):**
   - `documentation/plans/12_03_2026_1115_post_experiment_watcher_orchestrator.md` — Plan 2 spec
   - `documentation/plans/12_03_2026_1115_expand_dashboard_all_visualizations.md` — Plan 3 spec
   - `documentation/codebase_descriptions/how_this_codebase_works_12_03_2026.md` — Full codebase reference
   - `sbatch_template.sh` — Template for CPU experiment jobs (pattern for watcher script)
   - `calculate_metrics.py` — CLI for metrics calculation (to understand how to invoke in batch)
   - `analysis/march11_experiment_plots/dash_app.py` — Prototype standalone dashboard (ported failure/error plots from here)
   - `src/evaluation/visualization/failure_io.py` — `load_analysis_report()`, `build_all_runs_table()` functions
   - Explore agents read: `sbatch_template_gpu.sh`, `read_and_analyze_logs_and_traces_cli.py`, `make_standard_evaluation_plots.py`, all dashboard tab files, `plots.py`, components

4. Errors and Fixes:
   - **Shellcheck SC2181 (style)**: `if [[ $? -eq 0 ]]` in `launch_experiment.sh` — Fixed by restructuring to `if OUTPUT=$(sbatch "$script" 2>&1); then`
   - **Shellcheck SC2086 (info)**: Unquoted `$WATCHER_ARGS` — Fixed with `# shellcheck disable=SC2086` directive
   - **Shellcheck SC1073/SC1072 (error)**: Invalid shellcheck directive `# shellcheck disable=SC2086 -- comment` — The `--` comment syntax is not valid for shellcheck directives. Fixed by removing the `-- comment` part: `# shellcheck disable=SC2086`
   - **Ruff auto-fix**: Import ordering issues in `failure_analysis.py` and `error_analysis.py` (moved `import numpy as np` before `import pandas as pd`) — auto-fixed by ruff
   - **Ruff format**: 13 dashboard files reformatted by `ruff format` — these were included in the commit as legitimate formatting improvements
   - **Pre-commit ruff failure on commit**: After initial `git add`, ruff found one more issue in `data_loader.py` during the commit hook — re-staged the file and committed again successfully

   **User feedback on design:**
   - User asked: "Does this watcher script now run per job? so it does not make the total plots, only the per-experiment plots?" — Clarified that the watcher runs ONCE after ALL jobs complete and makes aggregate cross-experiment plots via the glob `results/*${EXPERIMENT_NAME}*/metrics.json`
   - User asked: "this EXPERIMENT_NAME is not in the results folder, is this a new subfolder made when submitting via this route?" — Improved the default experiment name inference in `launch_experiment.sh` to parse it from the job script's `RUN_RESULTS_DIR` line using grep, with fallback to the job script filename. Added explanatory comments about substring glob matching.

5. Problem Solving:
   - All tasks from both plans completed and committed
   - Tests verified: 83 passed, 4 pre-existing failures (unchanged) — no regressions introduced
   - Three commits on `fix/unified-result-dir-naming`:
     - `1de8c1e` — Plan 2: watcher and launcher scripts
     - `cfd0a5c` — Plan 3: dashboard expansion (13 files, 2301 insertions, 639 deletions)
     - `db6a2a5` — Codebase description update
   - User's Phoenix question answered: dashboard queries Phoenix via HTTP API (`px.Client(endpoint=...)` → `get_spans_dataframe()`), not via .phoenix folder

6. All User Messages:
   - Initial request: Invoked `/compound-engineering:ce:work` with detailed instructions for Plan 2 and Plan 3 implementation
   - "Does this watcher script now run per job? so it does not make the total plots, only the per-experiment plots?" — Asking about the watcher's scope
   - "this EXPERIMENT_NAME is not in the results folder, is this a new subfolder made when submitting via this route?" — Asking about how EXPERIMENT_NAME relates to results directory naming
   - "how does the dashboard get traces from phoenix. Does it load from the .phoenix folder?" — Asking about Phoenix data loading mechanism

7. Pending Tasks:
   - No explicit pending tasks. Both plans are fully implemented, committed, tested, and documented.
   - The plan files themselves have not been updated with checkboxes marked (the `ce:work` skill mentions marking `[ ]` → `[x]` in plan files, but this was not explicitly requested by the user).

8. Current Work:
   All implementation work is complete. The last actions were:
   - Committed the codebase description update (`db6a2a5`)
   - Answered the user's question about how the dashboard gets traces from Phoenix (via HTTP API, not file-based)
   - All 12 todo items marked completed

9. Optional Next Step:
   No immediate next step required — both plans are fully implemented and committed. The user may want to:
   - Push the branch to remote
   - Test the dashboard interactively
   - Run an experiment using `launch_experiment.sh` to verify the end-to-end watcher pipeline
   - But none of these were explicitly requested, so no action should be taken without user confirmation.

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /home/cog/dstoker/.REDACTED.jsonl

---

All right, can you make sure that output plots are now generated for all runs that were done yesterday (they may already exist) in the format that the dash dashboard expects, so that I can then check that it works properly? /ce:brainstorm 

First tell me:
- in what format does the dashboard expect the plots
- where are the plots now and which plots are there?
- are there traces in the .phoenix SQLite database corresponding to yesterday's runs?
- can we somehow run what the watcher script runs separately for the runs from yesterday? Not as a watcher jobs since the jobs have already ended
- what else should I take into account?

---

Yes do that, and launch the dashboard after.

---

Implement it.

---

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Summary:
1. Primary Request and Intent:
   The user's requests in this conversation were:
   
   **Request 1**: "Make sure that output plots are now generated for all runs that were done yesterday" — ensure the dashboard has data to display for March 11 experiment runs. First wanted a brainstorm answering 5 specific questions about dashboard data format, current plot state, Phoenix traces, standalone watcher execution, and other considerations.
   
   **Request 2**: "Yes do that, and launch the dashboard after" — run the watcher script for yesterday's runs and start the dashboard.
   
   **Request 3**: "So where will the post_experiment_analysis output plots?" — clarification question about output location.
   
   **Request 4**: Proposed separating `results/` (per-run data) from `analysis/` (per-experiment aggregate analysis), putting analysis output in `analysis/` directory instead of `results/`. Asked for brainstorm: what changes needed, smart or unwise? Referenced `/ce:brainstorm`.
   
   **Request 5**: "Implement it." — implement the separation of results/ and analysis/ directories.

2. Key Technical Concepts:
   - Dashboard generates all plots dynamically from data files (trace.json, metrics.json, analysis_report.json) — does NOT consume pre-rendered PNGs
   - Phoenix server running on port 6006 with 23 traces/737 spans from March 11, but `get_spans_dataframe()` hangs indefinitely requiring a timeout workaround
   - Watcher script (`run_post_experiment_analysis.sh`) can be run standalone without sbatch
   - `analysis_report.json` is the key missing piece for the Failure Analysis dashboard tab
   - Directory separation: `results/` for per-run outputs (immutable), `analysis/` for derived aggregate artifacts (regeneratable)
   - `DashboardDataLoader` constructor now accepts optional `analysis_base_dir` parameter
   - Both `results/` and `analysis/` are already gitignored

3. Files and Code Sections:

   - **`run_post_experiment_analysis.sh`**
     - Watcher script that chains log analysis → metrics calculation → plot generation
     - Changed default output dir from `results/analysis_*` to `analysis/analysis_*`
     - Two edits made:
       ```
       # Line 51: help text
       echo "  --output-dir <dir>            Output directory (default: analysis/analysis_<experiment>_<timestamp>/)"
       # Line 112: default output dir
       OUTPUT_DIR="analysis/analysis_${EXPERIMENT_NAME}_${TIMESTAMP}"
       ```

   - **`src/dashboard/data_loader.py`** (CRITICAL - partially modified, needs more work)
     - Constructor updated to accept `analysis_base_dir`:
       ```python
       def __init__(
           self,
           phoenix_endpoint: str,
           results_base_dir: Path,
           analysis_base_dir: Path | None = None,
       ):
           self.phoenix_endpoint = phoenix_endpoint
           self.results_dir = Path(results_base_dir)
           # analysis_dir defaults to analysis/ as sibling of results/
           self.analysis_dir = (
               Path(analysis_base_dir)
               if analysis_base_dir
               else self.results_dir.parent / "analysis"
           )
       ```
     - Phoenix timeout fix added to `_init_phoenix()`:
       ```python
       def _init_phoenix(self):
           if not _PHOENIX_AVAILABLE:
               return
           try:
               self._phoenix_client = px.Client(endpoint=self.phoenix_endpoint)
               import concurrent.futures
               with concurrent.futures.ThreadPoolExecutor(max_workers=1) as executor:
                   future = executor.submit(self._phoenix_client.get_spans_dataframe, limit=1)
                   future.result(timeout=10)
               self._phoenix_available = True
               logger.info("Phoenix connection established")
           except Exception as e:
               logger.warning(f"Phoenix not available: {e}")
               self._phoenix_available = False
               self._phoenix_client = None
       ```
     - **CRITICAL ISSUE**: The file currently ends at line 457. The following methods from Plan 3 are MISSING and need to be restored:
       - `get_analysis_report()` — needs to search analysis/ dir first, then results/ for backwards compat
       - `get_all_runs_with_failures()` — enriches runs with failure reasons
       - `get_error_breakdown()` — per-run error counts
       - `get_column_errors()` — per-column per-run error counts
     - Missing imports that were added in Plan 3: `infer_context` from `evaluation.visualization.enrich`, `build_all_runs_table` and `load_analysis_report` from `evaluation.visualization.failure_io`
     - `refresh()` method no longer clears `_analysis_report_cache`
     - The file appears to have been reverted/reformatted by the linter, losing Plan 3 additions

   - **`src/dashboard/app.py`** — needs `--analysis-dir` CLI argument and passing to DashboardDataLoader (not yet done)

   - **`launch_experiment.sh`** — has manual analysis help text that may reference `results/analysis_*` (not yet updated)

   - **`.gitignore`** — already has `analysis/` on line 30, no changes needed

   - **`results/analysis_20260311_20260312_1226/`** — created by watcher run, contains:
     - `analysis_report.json` (23 runs analyzed)
     - `plots/` (16 static seaborn plots)
     - `tables/` (5 CSV tables)
     - `analysis_complete.json` (summary)

4. Errors and fixes:
   - **Dashboard module path error**: `python -m dashboard.app` failed with `No module named 'dashboard'`. Fixed by using `PYTHONPATH=src .venv/bin/python src/dashboard/app.py`.
   - **Multiple dashboard processes**: Several zombie processes accumulated from failed starts. Fixed by killing all with `ps aux | grep "dashboard/app.py" | grep -v grep | awk '{print $2}' | xargs kill`.
   - **Phoenix `get_spans_dataframe()` hanging indefinitely**: The Phoenix server responds to health checks but span queries hang. Fixed by wrapping in `concurrent.futures.ThreadPoolExecutor` with 10-second timeout in `_init_phoenix()`.
   - **Linter reformatting data_loader.py**: When attempting to edit `get_analysis_report()`, got error "File has been modified since read, either by the user or by a linter." The linter appears to have reformatted the file AND the Plan 3 methods (get_analysis_report, get_all_runs_with_failures, get_error_breakdown, get_column_errors) along with their imports are now missing from the file.

5. Problem Solving:
   - Successfully ran watcher script standalone for yesterday's runs — produced analysis_report.json and plots
   - Successfully launched dashboard on port 8050 after fixing Phoenix timeout issue
   - Dashboard found 43 experiment runs from file-based discovery
   - Began implementing analysis/ directory separation but discovered data_loader.py lost Plan 3 methods during linter reformatting
   - The separation implementation is partially complete — watcher script updated, constructor updated, but get_analysis_report() and app.py changes still pending

6. All user messages:
   - "All right, can you make sure that output plots are now generated for all runs that were done yesterday (they may already exist) in the format that the dash dashboard expects, so that I can then check that it works properly? /ce:brainstorm First tell me: - in what format does the dashboard expect the plots - where are the plots now and which plots are there? - are there traces in the .phoenix SQLite database corresponding to yesterday's runs? - can we somehow run what the watcher script runs separately for the runs from yesterday? Not as a watcher jobs since the jobs have already ended - what else should I take into account?"
   - "Yes do that, and launch the dashboard after."
   - "So where will the post_experiment_analysis output plots?"
   - "I would prefer if the results dir /hpc/compgen/projects/llm_GEO_project/harmonia_metadata_agent/analysis/dstoker/harmonia/results remains for per-run results (e.g. LLM x context x task) and the analysis dir has results for the whole experiment. /hpc/compgen/projects/llm_GEO_project/harmonia_metadata_agent/analysis/dstoker/harmonia/analysis this analysis dir. What changes would have to be made to the codebase to make this possible? Is this a smart choice or very unwise? /ce:brainstorm"
   - "Implement it."

7. Pending Tasks:
   - **Restore missing Plan 3 methods in data_loader.py**: `get_analysis_report()`, `get_all_runs_with_failures()`, `get_error_breakdown()`, `get_column_errors()`, plus imports for `infer_context`, `build_all_runs_table`, `load_analysis_report`
   - **Update `get_analysis_report()`** to search `self.analysis_dir` first (analysis/analysis_*/analysis_report.json), then results/ for backwards compatibility
   - **Update `refresh()`** to clear `_analysis_report_cache`
   - **Add `--analysis-dir` CLI argument to `app.py`** and pass it through to DashboardDataLoader
   - **Update `launch_experiment.sh`** help text if it references analysis output location
   - **Restart the dashboard** after changes are complete (it's currently running on port 8050 with PID 1081019 but without the failure analysis methods)

8. Current Work:
   I was implementing the separation of `results/` and `analysis/` directories per the user's request "Implement it." I had:
   1. Updated `run_post_experiment_analysis.sh` default output dir (done)
   2. Updated `DashboardDataLoader.__init__()` to accept `analysis_base_dir` (done, preserved after linter)
   3. Was about to update `get_analysis_report()` to search the new `analysis/` dir first, but discovered the file lost all Plan 3 methods (get_analysis_report, get_all_runs_with_failures, get_error_breakdown, get_column_errors and their imports) during a linter reformat
   
   The last file read of data_loader.py (full file, 457 lines) confirmed the missing methods. The file ends at the `phoenix_available` property with no failure analysis or error analysis methods present.

9. Optional Next Step:
   Re-read data_loader.py to confirm current state, then restore all missing Plan 3 methods with the updated `get_analysis_report()` that searches `self.analysis_dir` first. The methods to restore are from the conversation summary's "Files Modified" section for data_loader.py. After restoring, update app.py with `--analysis-dir` argument and pass it to DashboardDataLoader. This directly continues the user's "Implement it." request for the analysis/ directory separation.

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /home/cog/dstoker/.REDACTED.jsonl
Continue the conversation from where it left off without asking the user any further questions. Resume directly — do not acknowledge the summary, do not recap what was happening, do not preface with "I'll continue" or similar. Pick up the last task as if the break never happened.

---

[Request interrupted by user for tool use]

---

<task-notification>
<task-id>b8g7uwjtc</task-id>
<tool-use-id>toolu_01P7hsKuQsBmFymNJHrPzQzN</tool-use-id>
<output-file>REDACTED.output</output-file>
<status>completed</status>
<summary>Background command "cd /hpc/compgen/projects/llm_GEO_project/harmonia_metadata_agent/analysis/dstoker/harmonia && grep -r "analysis/" --include="*.py" --include="*.sh" | grep -v ".venv" | grep -v ".pytest_cache" | head -40" completed (exit code 0)</summary>
</task-notification>
Read the output file to retrieve the result: REDACTED.output