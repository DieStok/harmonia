I want to fill out the folder /hpc/compgen/projects/llm_GEO_project/harmonia_metadata_agent/analysis/dstoker/harmonia/experiments/experiment_1_harmonia_dou2020_gdc/configs/manual with experiment configs that I can run one-by-one to manually do experiments with the different LLMs defined also for the automated experiments. For now, always use the anyllm backend to interact.

Could you:
- check the code that reads the config files and generates experiments based on them to see how it works 
- propose any changes needed to this code such that it also allows for .yaml files that just define things like the llm provider and the temperature but nothing else (so that they just start up the proper container running beaker that exposes its port for connection from the experimenter, but no automated script takes place)
- generate the manual config files 
- make sure these files reference the data/experiment descriptions in harmonia_metadata_agent/raw/datasets_harmonia where relevant. 



# AFTER:

Please automate .env file creation:
- just copy the .env file that exists
- make the edits based on the experiment config file
- save that .env file as [EXPERIMENT_CONFIG_FILE_NAME]_associated.env
- make sure these .env files get added to .gitignore
- edit harmonia_metadata_agent/analysis/dstoker/harmonia/exec_apptainer_harmonia.sh to allow reading of a specified .env file
- then make sure that for manual jobs the proper container with proper env files is run.

After all this please also:
- make an example manual experiment config template file called dou_harmonization_manual_config.template in the folder where you just made all these manual configs (to reference for future config files)
- update the config files in harmonia_metadata_agent/analysis/dstoker/harmonia/experiments/experiment_1_harmonia_dou2020_gdc/configs/automated to make sure that they are up to standard for the new/changed implementation of config.py ONLY IF NEEDED

