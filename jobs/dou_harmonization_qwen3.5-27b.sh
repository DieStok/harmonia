#!/bin/bash
# ---- AUTO-GENERATED by generate_jobs.py on 2026-03-12 11:58:45 ----
# Source config: experiments/experiment_1_harmonia_dou2020_gdc/configs/automated/dou_harmonization_qwen3.5-27b.yaml
# Template: sbatch_template.sh
# Re-generate with: python generate_jobs.py --config experiments/experiment_1_harmonia_dou2020_gdc/configs/automated/dou_harmonization_qwen3.5-27b.yaml
# ------------------------------------------------------------
# =============================================================================
# Harmonia Experiment SBATCH Template - CPU (Cloud LLM providers)
# =============================================================================
# This template is used by generate_jobs.py to create individual job scripts.
# Variables in {{double_braces}} are replaced by the generator.
#
# Usage:
#   sbatch jobs/experiment_gpt4o.sh
#
# Or generate jobs first:
#   python generate_jobs.py --config experiments/configs/dou_harmonization.yaml
#
# Note: For local LLM providers (ollama, anyllm:ollama), use sbatch_template_gpu.sh
#       or exec_apptainer_harmonia.sh will auto-start Ollama on CPU nodes too.
# =============================================================================

#SBATCH --job-name=harmonia_dou_harmonization_qwen3.5-27b
#SBATCH --output=/dev/null
#SBATCH --error=/dev/null
#SBATCH --time=02:00:00
#SBATCH --mem=20G
#SBATCH --cpus-per-task=2
#SBATCH --gres=tmpspace:50G

# =============================================================================
# Environment Setup
# =============================================================================

set -e

# Generate unique run ID for linking logs to results
RUN_ID=$(python3 -c "import secrets; print(secrets.token_hex(4))")
export RUN_ID
TIMESTAMP=$(date -u +%Y%m%d_%H%M%S)
RUN_RESULTS_DIR="/hpc/compgen/projects/llm_GEO_project/harmonia_metadata_agent/analysis/dstoker/harmonia/results/${TIMESTAMP}_dou_harmonization_qwen3.5-27b_${SLURM_JOB_ID}_${RUN_ID}"

# Redirect all output to date-stamped log files (includes run_id)
LOG_TIMESTAMP=$(date +%d-%m-%Y_%H%M)
LOG_OUT="logs/${LOG_TIMESTAMP}_dou_harmonization_qwen3.5-27b_${SLURM_JOB_ID}_${RUN_ID}.out"
LOG_ERR="logs/${LOG_TIMESTAMP}_dou_harmonization_qwen3.5-27b_${SLURM_JOB_ID}_${RUN_ID}.err"
mkdir -p logs
exec > "$LOG_OUT" 2> "$LOG_ERR"

echo "=============================================="
echo "Harmonia Experiment: dou_harmonization_qwen3.5-27b"
echo "=============================================="
echo ""
echo "Job ID: $SLURM_JOB_ID"
echo "Run ID: $RUN_ID"
echo "Node: $(hostname)"
echo "Date: $(date)"
echo ""

# Change to project directory
cd /hpc/compgen/projects/llm_GEO_project/harmonia_metadata_agent/analysis/dstoker/harmonia

# Dynamic port based on job ID to avoid conflicts
PORT=$((8100 + (SLURM_JOB_ID % 100)))
echo "Using port: $PORT"

# Create logs directory
mkdir -p logs

# =============================================================================
# Start Beaker Server with exec_apptainer_harmonia.sh
# =============================================================================
# The exec script handles:
# - Ollama auto-start for local LLM providers (ollama, anyllm:ollama)
# - Ollama logging to logs/ollama_<timestamp>.log
# - Model pre-loading/warming
# - Apptainer image selection (new harmonia image with litellm support)
# - Data and results directory binding
# - Environment variable configuration

echo ""
echo "Starting Beaker server on port $PORT..."
echo "LLM Provider: openrouter"
echo "LLM Model: qwen/qwen3.5-27b"
echo ""

# Start Beaker server via exec script (handles Ollama automatically if needed)
./exec_apptainer_harmonia.sh \
    --port $PORT \
    --config experiments/experiment_1_harmonia_dou2020_gdc/configs/automated/dou_harmonization_qwen3.5-27b.yaml \
    --job-name "dou_harmonization_qwen3.5-27b_${SLURM_JOB_ID}" \
    --results-dir "$RUN_RESULTS_DIR" \
    --run-id "$RUN_ID" &

SERVER_PID=$!
echo "Server PID: $SERVER_PID"

# Function to cleanup on exit
cleanup() {
    echo ""
    echo "Cleaning up..."
    if [ -n "$SERVER_PID" ]; then
        kill $SERVER_PID 2>/dev/null || true
        wait $SERVER_PID 2>/dev/null || true
    fi
    echo "Done."
}
trap cleanup EXIT

# Wait for server to be ready
echo "Waiting for server to start..."
MAX_WAIT=90  # Increased timeout for potential model loading
WAITED=0
while [ $WAITED -lt $MAX_WAIT ]; do
    if curl -s -o /dev/null -w "%{http_code}" "http://localhost:$PORT/api" 2>/dev/null | grep -q "200\|401"; then
        echo "Server is ready!"
        break
    fi
    sleep 2
    WAITED=$((WAITED + 2))
    echo "  Waiting... ($WAITED/$MAX_WAIT seconds)"
done

if [ $WAITED -ge $MAX_WAIT ]; then
    echo "ERROR: Server failed to start within $MAX_WAIT seconds"
    exit 1
fi

# =============================================================================
# Run Experiment
# =============================================================================

echo ""
echo "Running experiment..."
echo "Config: experiments/experiment_1_harmonia_dou2020_gdc/configs/automated/dou_harmonization_qwen3.5-27b.yaml"
echo ""

# Get token from env file
TOKEN=$(grep "^JUPYTER_TOKEN=" .env | cut -d '=' -f2)

# Run the experiment
.venv/bin/python run_experiment.py \
    --config experiments/experiment_1_harmonia_dou2020_gdc/configs/automated/dou_harmonization_qwen3.5-27b.yaml \
    --server http://localhost:$PORT \
    --token "$TOKEN" \
    --timeout 600

EXIT_CODE=$?

# =============================================================================
# Completion
# =============================================================================

echo ""
echo "=============================================="
if [ $EXIT_CODE -eq 0 ]; then
    echo "Experiment completed successfully!"
else
    echo "Experiment failed with exit code: $EXIT_CODE"
fi
echo "=============================================="

exit $EXIT_CODE
